pipeline {
    agent any

    stages {
        stage('Docker Build') {
            steps {
                sh '''
                    docker build -t my-playwright-netlify-app .
                    echo "Docker image built:"
                    pwd
                    docker images
                '''
            }
        }
    }
}